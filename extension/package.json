{
  "name": "@eloscope/extension",
  "version": "0.1.0-beta.28",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "fixture:build": "node scripts/build-fixture.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage.enabled=false"
  },
  "dependencies": {
    "@eloscope/core": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.8",
    "happy-dom": "^18.0.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
